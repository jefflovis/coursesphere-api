{
  "name": "coursesphere-api",
  "version": "1.0.0",
  "description": "API fake para plataforma CourseSphere",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
